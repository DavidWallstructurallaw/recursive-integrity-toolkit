name: Phase 3 Step 10 Hero Scaffold Contract

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  hero-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install approved test tools
        run: python -m pip install ".[test]"
      - name: Verify Hero and frozen calculation oracles without generating a report
        env:
          PYTHONDONTWRITEBYTECODE: "1"
        run: |
          python scripts/check_spec_consistency.py
          python -m pytest -p no:cacheprovider -q tests/integration/test_hero_structure.py tests/integration/test_phase3_metric_pipeline.py tests/golden/test_phase3_math.py
          python scripts/release_check.py --phase 3 --step 10 --diff
# No build_golden.py, normalize_golden.py or analytical report is executed.
