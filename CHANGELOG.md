# Changelog

## 0.1.0.dev2: Phase 3 mathematical milestone

Added scoped literal topic/label and exact record-form representations; exact duplicate groups/counts; unweighted and explicitly weighted distributions; support, Gini-Simpson diversity and Simpson concentration; source/confidence composition and retained coverage; direct closure-exposure intervals; four explicit tail rules and analytic extinction; expected contraction and explicitly seeded closed resampling; and explicit compatible ordered-pair support/diversity kernels.

Retained twenty independently authored mathematical oracles, exact/rational and stochastic checks, inherited test identities, all forty modules, sixteen protected placeholders, immutable Phase 0/Phase 2 records and no-network/input-only guarantees. Step 11 changes runtime only through the approved version literal, finalizes current documentation and checks current/minimum-compatible dependency profiles on Ubuntu/Windows with Python 3.11/3.12. Complete real-PyArrow regression, wheel/sdist checks, installed input/math smoke and a full tracked-source ZIP remain mandatory.

The two stage-test maintenance exceptions are explicitly approved and byte-bounded. Actual acceptance and limitations are recorded in the three `PHASE_3_*` milestone reports. Reports/audit CLI, general lineage/ancestry, longitudinal orchestration and external reopening remain separately gated. No stable publication, tag or main merge is included.

## Historical 0.1.0.dev1: Phase 2 input-validation milestone

Added explicit local CSV/JSONL and optional Parquet ingestion, file inventory and snapshot hashes; fourteen allowlisted declarative mapping operations; canonical field/identity checks; provenance attachment and separate validation coverage; explicit chronology, immediate parents and generation consistency; opt-in contained UTF-8 content reads; Level 0-5 input classification with seven independent capabilities; and one-call `validate_bundle` orchestration.

Step 10 adds complete core/real-Parquet CI, independent Security and Hero-contract gates, retained JUnit/log evidence, clean installed-wheel checks and a checksummed source archive. Actual acceptance is recorded in `PHASE_2_COMPLETION.md`.

Preserved original input bytes, missing/null/unknown distinctions, independent source/grounding/review declarations, earlier regression tests, twenty-six later-layer placeholders and licensing boundaries. Development version remains `0.1.0.dev1`; no stable release, registry publication or merge.

Still deferred: analytical metrics, state assignment, shares/bounds/deltas, general cycle detection, roots, lineage depth, ancestry, simulation execution and audit reports. Phase 3 requires a separate approved plan.

## Phase 1 repository scaffold

Added approved Phase 0 specifications, repository identity, package metadata, import-safe module layout, owner labels, tests/fixtures, five schemas, Hero inputs, licensing/security declarations and CI skeletons. Only startup help/version behavior was available at that milestone.
