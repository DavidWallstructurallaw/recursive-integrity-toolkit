# Third-Party Notices

The Phase 1 package metadata declares the following direct runtime dependencies:

- NumPy
- pandas

The optional Parquet extra declares:

- PyArrow

Development and release tooling may include pytest, pytest-cov, Hypothesis, jsonschema, Ruff, mypy, build, twine, and pip-audit.

Exact installed versions and license notices must be reviewed before an official release. This Phase 1 scaffold does not bundle third-party source code, models, datasets, or fonts.
