{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://recursive-integrity-toolkit.example/schemas/version_order.schema.json",
  "title": "Recursive Integrity Toolkit Version Order",
  "description": "Phase 2 structural contract for an explicit ordered list of dataset versions. Conflict resolution and alternative ordering evidence are validated in a later Phase 2 step.",
  "type": "object",
  "required": ["version_order"],
  "properties": {
    "version_order": {
      "type": "array",
      "description": "Dataset versions in user-declared chronological order.",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    }
  },
  "additionalProperties": false
}
